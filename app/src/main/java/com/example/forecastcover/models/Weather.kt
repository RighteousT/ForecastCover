package com.example.forecastcover.models

// Placeholder data classes (will be expanded later in ViewModel task)
data class Weather(
    val condition: String,
    val temperature: Int,
    val precipitation: String,
    val wind: String
)
